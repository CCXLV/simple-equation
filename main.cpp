#include <iostream>

int main() {
    int l;
    int t;

    int length;
    int first;
    int second;

    length = 6 * l;
    first = t;
    second = t + 5;

    l = (length / first) + (length / second);

    std::cout << l;

    return 0;
}